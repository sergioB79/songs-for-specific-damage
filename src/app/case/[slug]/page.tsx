import { notFound } from "next/navigation";
import Link from "next/link";
import { axes,caseSlug,cases,getCaseBySlug,spotifyId } from "@/lib/data";
export function generateStaticParams(){return cases.map((item)=>({slug:caseSlug(item)}))}
export default async function CasePage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const item=getCaseBySlug(slug);if(!item)notFound();const related=cases.filter((other)=>other.case_id!==item.case_id&&(other.primary_damage===item.primary_damage||other.secondary_damage===item.primary_damage)).slice(0,3);return <main className="case-page"><div className="eyebrow">{item.case_id} // CASE FILE // {item.status}</div><div className="case-layout"><div><h1>{item.title}</h1><p className="case-desc">{item.ssd_description}</p><iframe className="spotify-frame" src={`https://open.spotify.com/embed/playlist/${spotifyId(item.spotify_url)}`} allow="autoplay; clipboard-write; encrypted-media; fullscreen; picture-in-picture" loading="lazy" title={`Spotify playlist: ${item.title}`}/></div><aside className="report"><div className="eyebrow">AUDIO PSYCHOLOGY UNIT</div><div className="report-row"><b>Primary damage</b><div>{item.primary_damage}</div></div><div className="report-row"><b>Secondary damage</b><div>{item.secondary_damage}</div></div><div className="report-row"><b>Diagnosis</b><div>{item.diagnosis_label}</div></div><div className="report-row"><b>Symptoms</b><div>{item.symptoms}</div></div><div className="report-row"><b>Dosage</b><div>{item.dosage}</div></div><div className="report-row"><b>Prognosis</b><div>{item.prognosis}</div></div><div className="report-row"><b>Damage intensity</b><div>{item.damage_intensity}%<div className="meter"><span style={{width:`${item.damage_intensity}%`}}/></div></div></div><div className="axis-grid">{axes.map((axis)=><div key={axis}><span>{axis}</span><b>{item[axis]}</b></div>)}</div></aside></div><section className="section related"><div className="section-head"><h2>Alternative treatments</h2><Link href="/index" className="micro">RETURN TO INDEX →</Link></div><div className="manifesto-grid">{related.map((other)=><article className="note-card" key={other.case_id}><span className="case-id">{other.case_id}</span><h3>{other.title}</h3><p>{other.ssd_description}</p><Link className="micro" href={`/case/${caseSlug(other)}`}>OPEN CASE →</Link></article>)}</div></section></main>}
